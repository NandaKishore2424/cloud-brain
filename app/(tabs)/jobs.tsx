import { ApplicationsScreen } from '@/features/applications/components/ApplicationsScreen';

/** Route file. Thin by convention — see CLAUDE.md §3. */
export default function JobsRoute() {
  return <ApplicationsScreen />;
}
